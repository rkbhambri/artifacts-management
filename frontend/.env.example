# URL of the backend API as reached from the browser
NEXT_PUBLIC_API_URL=http://localhost:4000

# Demo-only key for the dashboard upload form (see README security notes)
NEXT_PUBLIC_API_KEY=dev-internal-api-key
